# Decisions ledger

Every decision with its reason. `[owner]` = owner decision (do not change; material
changes go to `OWNER-QUESTIONS.md`). `[lead]` = technical decision by the orchestrating
session, may be revised with evidence (append a new entry, do not rewrite history).

**Frozen ledger of the first slice (2026-09-19 to 2026-09-20).** New decisions are
Architecture Decision Records in `docs/adr/` ([index](docs/adr/README.md)); this table is not
amended.

| # | Date | Kind | Decision | Reason | ADR |
|---|---|---|---|---|---|
| D1 | 2026-09-19 | owner | p1 is a new project; Iris is a parts donor. One small core + modules; tools are their own modules; providers translate only; harness reshapes around the model (Claude and GPT first); delegation optional; Rust, lean, descriptive names; first slice already modular. | Handoff §2, discovery notes. | [ADR-0001](docs/adr/0001-p1-is-a-new-project-iris-is-a-parts-donor.md), [ADR-0002](docs/adr/0002-one-small-core-tools-and-providers-as-modules.md), [ADR-0003](docs/adr/0003-harness-reshapes-itself-around-the-model.md), [ADR-0026](docs/adr/0026-delegation-is-optional.md) |
| D2 | 2026-09-19 | lead | New repo at `projects/phaseone`, branch `main` holds only the initial scaffold commit; all work on task branch `slice-1`. No remotes, no pushes. | Handoff §3: work on a task branch, no merge into main without the owner. | [ADR-0009](docs/adr/0009-new-repo-task-branch-no-remotes.md) |
| D3 | 2026-09-19 | lead | License MIT (copied from iris-agent `LICENSE`, same owner). Iris `NOTICE` lists Codex-derived Apache-2.0 files only under `src/ui/tui/streaming/*`, which p1 does not take; any donor file carrying an SPDX Apache header keeps it and gets a `NOTICE` entry. | Handoff §6 license check; iris-agent@62c8345 `NOTICE`. | [ADR-0006](docs/adr/0006-mit-licence-and-donor-provenance.md) |
| D4 | 2026-09-19 | lead | Crate prefix `p1-`; one crate per module boundary under `crates/`. Core isolation is enforced by `scripts/check-core-isolation.sh` on the resolved `cargo tree`, inside the gate. | seams.md §10 first acceptance item must be a runnable command, not a convention. | [ADR-0002](docs/adr/0002-one-small-core-tools-and-providers-as-modules.md) |
| D5 | 2026-09-19 | lead | Live provider checks require `P1_LIVE=1`, are run only by the lead, and are not part of the gate. | Handoff §3 secrets/live rules. | [ADR-0012](docs/adr/0012-live-provider-checks-are-lead-only.md) |
| D6 | 2026-09-19 | owner | p1 is developed in the open as a public GitHub repo `5omeOtherGuy/phaseone`. This supersedes D2's "no remotes, no pushes": development is trunk-based on `main`, with short-lived `task/*` branches merged as soon as the gate is green. No review gate, no branch protection. | Owner direction to optimise the repo for rapid development by several agents at once. | [ADR-0010](docs/adr/0010-public-repo-trunk-based-with-pushes-authorised.md) |
| D7 | 2026-09-19 | lead | CI is one job that runs `scripts/gate.sh`, on pushes to `main` and on pull requests. There are no other workflows, no matrices and no separate review/audit bots; the gate script stays the single definition of green. | D6; one check that cannot drift from the local gate. | [ADR-0011](docs/adr/0011-gate-is-the-single-definition-of-green.md) |
| D8 | 2026-09-19 | lead | Task state lives in GitHub Issues (labels `ready`, `in-progress`, `blocked`, `owner`); each task gets its own git worktree via `scripts/new-worktree.sh`. `STATUS.md` and `OWNER-QUESTIONS.md` are removed — they duplicated the board and every agent edited them, so they conflicted rather than informed. | Swarm coordination needs one shared truth per concern; files every agent rewrites are contention points. | [ADR-0007](docs/adr/0007-task-state-on-github-issues-and-worktrees.md) |
| D9 | 2026-09-19 | lead | The design baseline (`pillars.md`, `design-summary.md`, `seams.md`) is copied into `docs/design/` so the public repo is self-contained; `projects/phaseone-collab/` stays the working copy outside it. | D6: public readers cannot follow links to the owner's workstation. | [ADR-0005](docs/adr/0005-design-baseline-copied-into-the-repo.md) |
| D10 | 2026-09-19 | lead | Installed rustup components `rustfmt` and `clippy` for the stable toolchain (user-level, `~/.rustup`, no apt/sudo). | The required gate (fmt check + clippy -D warnings) could not run: `cargo-fmt is not installed for the toolchain stable`. Not speculative; nothing system-wide changed. | [ADR-0008](docs/adr/0008-rustfmt-and-clippy-for-the-gate.md) |
| D11 | 2026-09-19 | owner | Pushing and merging are authorised: "disregard the handoff when it comes to no push no merge, push and merge, this is your project." The lead pushes task branches and merges into `main` when the gate is green. | Owner message to the lead session, 2026-09-19; supersedes handoff §3/§8 on pushes and merges. Still not authorised: new billing, purchased capacity, publishing credentials or private prompts. | [ADR-0010](docs/adr/0010-public-repo-trunk-based-with-pushes-authorised.md) |
| D12 | 2026-09-19 | owner+lead | One shared cargo target dir for every checkout and worktree (`../phaseone-target`), written into an untracked `.cargo/config.toml` by `scripts/local-cargo-config.sh`; `jobs = 2`, incremental off, no debug info for dependencies. Cargo's build-directory lock serialises compilation across all agents. Supersedes the handoff's "each worktree has its own `target/`". Independent test authors work against supplied binaries and do not build. | Owner: small SSD must not fill with Rust targets, and concurrent compiles will exhaust 7 GB RAM. A shared dir stores dependencies once and makes "one compile at a time" a property of the machine instead of a rule agents must remember. | [ADR-0013](docs/adr/0013-shared-cargo-target-dir.md), [ADR-0014](docs/adr/0014-per-worktree-targets-and-rustc-semaphore.md) |
| D13 | 2026-09-19 | lead | Amends D8: `STATUS.md` stays, edited ONLY by the lead session, as its compaction-safe resume record (done / in progress / next / worker runs). Task claiming stays on GitHub Issues; owner questions are issues labelled `owner` and `OWNER-QUESTIONS.md` is removed. | The lead must recover state after context compaction without network access (observed failure F6: forgotten decisions). A single-writer file is not a contention point. | [ADR-0007](docs/adr/0007-task-state-on-github-issues-and-worktrees.md) |
| D14 | 2026-09-20 | owner+lead | Workers are fanned out DIRECTLY by `scripts/fanout.py` (a job list → `pi-worker` processes → one JSON summary), launched as one background task whose exit re-invokes the lead. No relay/supervisor model. The Workflow tool is not used for worker dispatch: its scripts can only spawn Claude agents and have no process access, so a non-Claude worker would need a Claude relay, which the owner rejected. Verified 2026-09-20 with one deepseek job (run `20260920-000458-1394036`, $0.0006, completion notification received). | Owner: "I don't like haiku relays … use our workers directly." | [ADR-0027](docs/adr/0027-workers-dispatched-directly-with-a-bounded-pool.md) |
| D15 | 2026-09-20 | lead | Worker pool: any queue length, at most 6 live workers, and no new worker starts below 1500 MB MemAvailable. Not 20 live. | Measured 115–190 MB resident per pi worker (20 ≈ 3–3.5 GB) against ~2.3 GB available with 4.4 GB already swapped; compiles are serialised anyway (D12) and lead acceptance is the real bottleneck. Revisit with measurements, not by raising the number. | [ADR-0027](docs/adr/0027-workers-dispatched-directly-with-a-bounded-pool.md) |
| D16 | 2026-09-20 | owner | The lead has full authority for this project while the owner is away and is not to stop for questions; actions blocked by the lead harness's permission classifier may be handed to a deepseek worker. The lead does not use that route around safety-relevant blocks (destructive operations, credentials). | Owner message, 2026-09-20. | [ADR-0028](docs/adr/0028-lead-authority-while-owner-is-away.md) |
| D17 | 2026-09-20 | lead | Contracts (`p1-contracts`): flat ordered history items with ordered assistant blocks; raw tool input (`Json`/`Text`) validated only at the tool; declarations `Function`/`Freeform`; opaque versioned `ReplayData` tagged with its origin on each reasoning block; `Usage` fields all `Option` and kept distinct; one terminal `StreamEvent::Finished`; boxed `Send` futures instead of `async-trait`; tools get their workspace/services by constructor, not through a context bag. | Each choice is forced by a difference between the two real routes — table in `docs/design/routes.md` §C. | [ADR-0015](docs/adr/0015-send-capable-boxed-future-contracts.md), [ADR-0016](docs/adr/0016-flat-history-and-tool-input-at-the-tool.md), [ADR-0017](docs/adr/0017-provider-stream-contract-and-conformance-suite.md), [ADR-0018](docs/adr/0018-reasoning-replay-opaque-and-configured-origin.md), [ADR-0019](docs/adr/0019-usage-and-cost-reporting.md) |
| D18 | 2026-09-20 | lead | Authorization outcome at the core is `Permit`/`Deny`; "ask" is resolved inside the host's policy implementation. Tools run strictly sequentially in this slice. The core awaits a running tool on cancellation instead of dropping it. | Keeps UI out of the core (seams §6); a dropped tool future leaves side effects unknown, an awaited one reports them. Parallel tools are an optimisation with ordering consequences — not needed to prove the architecture. | [ADR-0023](docs/adr/0023-cancellation-precedence-and-sequential-tools.md), [ADR-0024](docs/adr/0024-authorization-permit-deny-at-the-core.md) |
| D19 | 2026-09-20 | lead | Red-first acceptance suites are committed on the implementing task branch, never on `main`; `main` always has a green gate. | A red suite on `main` makes every other worker's "gate green" criterion unreachable and turns CI red (lead error on 2026-09-20, corrected one commit later). | [ADR-0011](docs/adr/0011-gate-is-the-single-definition-of-green.md), [ADR-0029](docs/adr/0029-test-first-with-independent-authors-and-frozen-suites.md) |
| D20 | 2026-09-20 | lead | Supersedes the mechanism of D12 (its goals stand). Each worktree builds into its OWN `target/`, seeded by `cp -al` (hardlinks) from the main checkout's `../phaseone-target` with all workspace-member artifacts removed; RAM is bounded by `scripts/rustc-serial`, a rustc wrapper holding one of two machine-wide `flock` slots for the life of each compilation. | The shared target dir was UNSOUND: cargo derives a workspace member's artifact names from its path relative to the workspace root, so every worktree's `p1-core` mapped to the same files and freshness was judged by mtime — a worker's gate linked another worktree's stale `unimplemented!` stub (assembly handoff, run `.worker-runs/20260920-004…` "environment flake"). False greens were possible; `main` itself was protected by CI, which builds clean and was green for every merge. Measured: seeding 0.4 s, 0 bytes extra disk for dependencies, 4 p1 crates rebuilt in 3.6 s. | [ADR-0014](docs/adr/0014-per-worktree-targets-and-rustc-semaphore.md) |
| D21 | 2026-09-25 | owner | The TUI has no code-size budget: the 6,000-line cap in the #12 plan is void. Iris features are ported on their merits when the owner wants them; mouse input comes first (#185). Dependencies stay minimal. | Owner, 2026-09-25: "The 6000 line budget does not exist anymore. There is not budget cap on the amount of code for our TUI."; "I would definetly like minimal dependencies"; on mouse: "iris-agent already handled all these issues we just need to look what iris does, review it, maybe optimize it and port it." Lead note: the cap was the #12 plan's stated scale argument (comment 2026-09-20, Kimi TUI session, accepted by the lead) for dropping Iris features including mouse; adding a crate absent from the workspace still needs asking first (AGENTS.md). | [#12](https://github.com/5omeOtherGuy/phaseone/issues/12), [#185](https://github.com/5omeOtherGuy/phaseone/issues/185) |
| D22 | 2026-09-25 | owner | p1 migrates completely to WebAssembly modules. Owner, verbatim: "p1 is going to migrate completly to wasm." "The Core can be swappable, but that doesn't mean we want to be the ones swapping it. I am happy with our pi core." "We do not need the browser GUI now, we can keep it in the terminal for now." "we will create an AWS ec2 machine to run the builds on. Just fot that project." The agent core, host, journal, worker service and terminal UI stay native; every tool, provider and policy becomes a WebAssembly module the host loads by name; a dedicated AWS EC2 machine runs this project's builds. Astra (gpt-6-astra, xhigh, Codex CLI, read-only) writes the migration plan. Supersedes compile-time composition (ADR-0004). | Owner messages to the lead session, 2026-09-25, after a comparison with DeepSeek Harness (`dsh`). | [ADR-0070](docs/adr/0070-p1-migrates-to-webassembly-modules-native-core-and-host-load-tools-providers-and-policies-by-name.md) |
