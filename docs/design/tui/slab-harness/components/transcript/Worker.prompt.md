One delegated worker; stack several inside a delegate Block with a blank BLOCK row between.
```jsx
<Worker name="s1-cache" route="deepseek · v4.1-flash" elapsed="1m12s" cost="$0.02" owns="spikes/s1/**" activity="cargo fetch — resolving 38 crates" />
```
