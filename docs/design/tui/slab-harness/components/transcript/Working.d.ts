/** The only animation: three ▪ cells, 1.1s cycle, 0.18s stagger. Lives in the running block's outcome slot. */
export interface WorkingProps {}
export function Working(props: WorkingProps): JSX.Element;
