//! The WebAssembly package loader's catalog entry point: the package loader
//! (ADR-0071) registers the modules it loaded here, beside the compiled keys.
//! Slice S1.4 fills it; until then the catalog registers no loaded module.
