# What the model is, independent of the route that reaches it (ADR-0039). The ChatGPT/
# Codex subscription endpoint serves this model with every effort the Codex CLI model
# cache listed for this account (2026-09-21): low, medium, high, xhigh.
id       = "gpt-5.5"
revision = 1
model_id = "gpt-5.5"
family   = "gpt"
thinking = "effort-level"

efforts = ["low", "medium", "high", "extra_high"]

# default_effort: absent — an effort-less request carries no reasoning fields.
# context_tokens and max_output_tokens: unknown for this model, so stated nowhere.
