# What the model is, independent of the route that reaches it (ADR-0039). The
# responses adapter's old rule — every model name took an effort level, and only
# low/medium/high; an effort-less request carried no reasoning fields — is what this
# profile records, byte for byte.
id       = "gpt-5.6-sol-mini"
revision = 1
model_id = "gpt-5.6-sol-mini"
family   = "gpt"
thinking = "effort-level"

# The adapter sent no reasoning fields without an explicit effort and rejected
# extra_high/max outright, so there is no default_effort and no higher lane.
efforts = ["low", "medium", "high"]

# context_tokens and max_output_tokens: unknown for this model, so stated nowhere.
